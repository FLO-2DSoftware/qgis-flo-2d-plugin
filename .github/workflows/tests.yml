name: Auto Tests
on: [push]
jobs:
  linux_tests:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u your-github-username --password-stdin

      - name: Pull Docker image from GitHub Container Registry
        run: docker pull ghcr.io/your-github-username/qgis-plugin-test

      - name: Run tests in Docker
        run: |
          docker run --rm \
            -e QT_QPA_PLATFORM=offscreen \
            -e QGIS_AUTH_DB_DIR_PATH=./auth \
            ghcr.io/your-github-username/qgis-plugin-test
